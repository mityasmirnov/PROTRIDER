# path to output directory to store result files
out_dir: output

# csv input file containing intensities. Columns are samples and rows are proteins.
input_intensities: sample_data/protrider_sample_dataset.tsv

# column name in the input file indicating the column name containing the protein names/IDs
index_col: "protein_ID"

# csv file containing sample annotations
# sample_annotation: sample_data/sample_annotations.tsv

## Preprocessing params
# maximum percentage of missing values per protein
max_allowed_NAs_per_protein: 0.3
# transformation funtion to apply to the data before model fitting. options are log10, log2, and log
# set this to null if the data is already log-transformed
log_func_name: "log"

# names of columns in the sample annotation file to be passed as covariates to the autoencoder.
# leaving this empty results in no covariates passed to the model
cov_used:
# - SEX
# - AGE
# - "BATCH_RUN"

# seed parameter for reproducibility
seed: 42

## Grid search outlier injection params
inj_freq: 1e-3
inj_mean: 3
inj_sd: 1.6
gs_epochs: 100

## Model params
autoencoder_training: True ## when set to false, it returns values after (PCA) initialization without training

n_layers: 1 # number of layers for the autoencoder model
n_epochs: 100 # number of training epochs
lr: 1e-4 # learning rate
batch_size:

find_q_method: "OHT" # option to find latent space dimension. Options: OHT, gs or an integer value
init_pca: True # PCA initialization
h_dim: # hidden dimension, only for n_layers>1

## Presence absence modelling
presence_absence: False
lambda_presence_absence: 0.5

## Stat params
pval_dist: 't' # Distribution choice for P-value computation. Options: gaussian, t
pval_adj: 'by' # Multiple testing correction. Options: by, bh
pval_sided: 'two-sided'
common_degrees_freedom: False # whether to use common degrees of freedom; more stable for small datasets
pseudocount: 0.01 # for FC computation

## Reporting params
outlier_threshold: 0.1
report_all: True

verbose: False
device: 'gpu'

## Model checkpoint
# Path to save/load model checkpoint (optional)
# If None: saves to out_dir/model.pt (or model_fold_N.pt for CV)
# If specified and exists: loads model from this path and skips training
# If specified and doesn't exist: trains and saves to this path
# checkpoint_path: 'checkpoint.pt'

use_wandb: False
# wandb_project: 'protrider_test'
# wandb_name: 'test_run'

## Cohort stability analysis
# Repeatedly remove random samples, rerun full PROTRIDER OHT including preprocessing,
# and aggregate metric stability into protrider_summary_bs.csv (subsampling, not classical bootstrap).
# cohort_stability: False
# cohort_stability_n_runs: 100
# cohort_stability_min_runs: 30
# cohort_stability_max_runtime_min:
# cohort_stability_drop_fraction: 0.1
# cohort_stability_min_samples: 30
# cohort_stability_seed:
# cohort_stability_require_oht: True
# cohort_stability_save_iteration_files: False
